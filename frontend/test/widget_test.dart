import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads smoke test', (WidgetTester tester) async {
    // Placeholder: App requires AuthBloc with async init
    expect(true, isTrue);
  });
}
